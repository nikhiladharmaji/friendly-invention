/**
 * TournamentConfig.js
 *
 * @description :: A model definition represents a database table/collection.
 * @docs        :: https://sailsjs.com/docs/concepts/models-and-orm/models
 */

module.exports = {

  attributes: {
    key: {
      type: 'string'
    },
    value: {
      type: 'string'
    }

    //  ╔═╗╦═╗╦╔╦╗╦╔╦╗╦╦  ╦╔═╗╔═╗
    //  ╠═╝╠╦╝║║║║║ ║ ║╚╗╔╝║╣ ╚═╗
    //  ╩  ╩╚═╩╩ ╩╩ ╩ ╩ ╚╝ ╚═╝╚═╝


    //  ╔═╗╔╦╗╔╗ ╔═╗╔╦╗╔═╗
    //  ║╣ ║║║╠╩╗║╣  ║║╚═╗
    //  ╚═╝╩ ╩╚═╝╚═╝═╩╝╚═╝


    //  ╔═╗╔═╗╔═╗╔═╗╔═╗╦╔═╗╔╦╗╦╔═╗╔╗╔╔═╗
    //  ╠═╣╚═╗╚═╗║ ║║  ║╠═╣ ║ ║║ ║║║║╚═╗
    //  ╩ ╩╚═╝╚═╝╚═╝╚═╝╩╩ ╩ ╩ ╩╚═╝╝╚╝╚═╝

  },
  getCurrentRound: async function () {
    var currentRound = await TournamentConfig.findOne({key: 'currentRound'});
    if(!currentRound) {
      return 1;
    }

    return parseInt(currentRound.value)
  },
  updateCurrentRound: async function(currentRound) {
    let result;
    if(currentRound == 2) {
      result = await TournamentConfig.create({ key: 'currentRound', value: currentRound });
    } else {
      result = await TournamentConfig.update({ key: 'currentRound' }, { value: currentRound });
    }
    
    return result;
  }

};

